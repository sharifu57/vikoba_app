import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF0B6B50);
  static const secondary = Color(0xFFD99A2B);
  static const accent = Color(0xFFEF6C4D);

  // Light Theme
  static const background = Color(0xFFF7F7F2);
  static const surface = Colors.white;
  static const card = Colors.white;

  // Dark Theme
  static const darkBackground = Color(0xFF10241D);
  static const darkSurface = Color(0xFF17372B);
  static const darkCard = Color(0xFF17372B);

  // Text
  static const textPrimary = Color(0xFF111827);
  static const textSecondary = Color(0xFF6B7280);

  static const darkTextPrimary = Colors.white;
  static const darkTextSecondary = Color(0xFFCBD5E1);

  // Status
  static const success = Color(0xFF22C55E);
  static const warning = Color(0xFFF59E0B);
  static const error = Color(0xFFEF4444);
  static const info = Color(0xFF3B82F6);

  // Borders
  static const border = Color(0xFFE5E7EB);
  static const darkBorder = Color(0xFF334155);
}
